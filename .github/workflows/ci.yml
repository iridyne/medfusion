name: CI/CD Pipeline

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled CI
        run: |
          echo "Repository-level CI is intentionally disabled."
          echo "This project can run normally without GitHub Actions verification."
