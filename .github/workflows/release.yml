name: Release

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled release automation
        run: |
          echo "Automatic release workflow is intentionally disabled."
          echo "Build and publish releases manually when needed."
