# Testing Configs

这里放的是脚本测试、smoke test、simulation test 用的配置。

它们的目标是：

- 验证训练链路是否能跑
- 验证可插拔性
- 验证 demo / CI / 本地排查流程

不建议把这里的配置当作 README 主入口。

## 当前文件

- `test_mock.yaml`
- `test_mobilenet.yaml`
- `simulation_test.yaml`
