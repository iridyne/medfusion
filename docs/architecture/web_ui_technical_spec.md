# MedFusion Web UI - 优化技术方案

## 🎯 设计原则

基于你的需求，我们的核心原则是：

1. **性能优先** - 前后端都追求极致性能
2. **高度可定制** - UI 主题和风格可切换
3. **一站式全流程** - 从数据到部署的完整链路
4. **本地优先** - 本地运行 + Docker 容器化
5. **高扩展性** - 插件化架构，易于扩展

---

## 🚀 技术栈（性能优化版）

### 前端技术栈

```yaml
核心框架:
  - React 18+ (并发模式，性能优化)
  - TypeScript (类型安全)
  - Vite (极速构建，HMR)

节点编辑器:
  - React Flow (高性能节点渲染)
  - 虚拟滚动优化
  - Canvas 渲染加速

UI 组件库（可切换）:
  - Ant Design (默认，企业级)
  - Chakra UI (备选，现代化)
  - 自定义主题系统

状态管理:
  - Zustand (轻量高效)
  - React Query (服务端状态缓存)

图表可视化:
  - ECharts (高性能，功能强大)
  - Canvas 渲染
  - 数据采样和降采样

实时通信:
  - Socket.IO (自动降级，可靠)
  - 消息压缩和批处理
  - 心跳检测

性能优化:
  - Web Worker (后台计算)
  - IndexedDB (本地缓存)
  - 虚拟列表 (react-window)
  - 懒加载和代码分割
  - Service Worker (PWA)
```

### 后端技术栈

```yaml
Web 框架:
  - FastAPI (异步高性能)
  - Uvicorn (ASGI 服务器)
  - 多进程 + 多线程

数据库:
  - SQLite (本地开发，简单)
  - PostgreSQL (生产环境，高性能)
  - SQLAlchemy (ORM)
  - Alembic (数据库迁移)

缓存:
  - Redis (内存缓存)
  - 缓存训练状态
  - 缓存模型元数据

任务队列:
  - Celery (分布式任务)
  - Redis (消息代理)
  - 支持优先级队列
  - 任务结果缓存

实时通信:
  - FastAPI WebSocket
  - Socket.IO (备选)
  - 消息压缩

性能优化:
  - 异步 I/O (asyncio)
  - 连接池 (数据库、Redis)
  - 响应缓存
  - 静态文件 CDN
  - Gzip 压缩
```

### 核心框架集成

```yaml
MedFusion 集成:
  - 直接调用 med_core API
  - 异步任务包装
  - 进度回调机制
  - 错误处理和恢复

监控:
  - GPU 监控 (pynvml)
  - 系统资源 (psutil)
  - 训练指标收集
  - 日志聚合
```

---

## 🎨 UI 主题系统

### 主题切换架构

```typescript
// 主题配置
interface Theme {
  name: string;
  colors: {
    primary: string;
    background: string;
    text: string;
    // ... 更多颜色
  };
  spacing: {
    unit: number;
    // ... 间距配置
  };
  typography: {
    fontFamily: string;
    // ... 字体配置
  };
}

// 预设主题
const themes = {
  light: { /* 浅色主题 */ },
  dark: { /* 深色主题 */ },
  blue: { /* 蓝色主题 */ },
  purple: { /* 紫色主题 */ },
};

// 设计风格切换
const designSystems = {
  antd: AntDesignComponents,
  chakra: ChakraUIComponents,
  custom: CustomComponents,
};
```

### 可定制项

```yaml
主题:
  - 浅色主题 (默认)
  - 深色主题
  - 高对比度主题
  - 自定义主题 (JSON 配置)

设计风格:
  - Ant Design (企业级)
  - Chakra UI (现代化)
  - 自定义组件库

布局:
  - 左侧节点库 (可折叠)
  - 右侧属性面板 (可调整宽度)
  - 底部日志面板 (可隐藏)
  - 全屏模式

字体:
  - 系统默认
  - 自定义字体
  - 字体大小调节

动画:
  - 完整动画
  - 减少动画
  - 无动画 (性能模式)
```

---

## 🔧 一站式全流程功能

### 完整工作流

```
数据准备 → 模型构建 → 训练配置 → 训练执行 → 
评估分析 → 模型优化 → 模型导出 → 部署推理
```

### 详细功能模块

#### 1. 数据管理模块

```yaml
数据集管理:
  - 上传数据集 (支持拖拽)
  - 数据集预览 (图像、表格)
  - 数据统计分析
  - 数据集版本管理
  - 数据集分割 (训练/验证/测试)

数据预处理:
  - 图像预处理 (Resize、Normalize、等)
  - 数据增强 (Augmentation)
  - 自定义预处理管道
  - 预处理预览

数据标注:
  - 简单标注工具
  - 导入外部标注
  - 标注质量检查
```

#### 2. 模型构建模块

```yaml
Backbone 选择:
  - 29+ 预训练模型
  - 模型对比工具
  - 参数量/FLOPs 显示
  - 预训练权重管理

融合策略:
  - 5 种融合方法
  - 可视化融合结构
  - 自定义融合层

分类头:
  - 多种头部类型
  - 自定义输出层
  - 多任务学习支持

完整模型:
  - 模型结构可视化
  - 模型摘要
  - 模型验证
```

#### 3. 训练配置模块

```yaml
训练器配置:
  - 基础训练器
  - 多模态训练器
  - 分布式训练器
  - 渐进式训练器

优化器:
  - Adam/AdamW/SGD/等
  - 学习率配置
  - 权重衰减
  - 梯度裁剪

学习率调度:
  - StepLR
  - CosineAnnealing
  - ReduceLROnPlateau
  - 自定义调度器

损失函数:
  - CrossEntropy
  - FocalLoss
  - 自定义损失
  - 多任务损失

高级配置:
  - 混合精度训练 (AMP)
  - 梯度检查点
  - 梯度累积
  - Early Stopping
  - 模型检查点
```

#### 4. 训练执行模块

```yaml
训练控制:
  - 开始训练
  - 暂停/恢复
  - 停止训练
  - 调整超参数 (动态)

实时监控:
  - 训练曲线 (Loss/Acc/LR)
  - 验证指标
  - 系统资源 (GPU/CPU/内存)
  - 训练速度 (samples/sec)
  - 预计剩余时间

日志管理:
  - 实时日志输出
  - 日志过滤
  - 日志导出
  - 错误高亮

快照管理:
  - 自动保存检查点
  - 最佳模型保存
  - 检查点恢复
  - 检查点对比
```

#### 5. 评估分析模块

```yaml
模型评估:
  - 测试集评估
  - 多指标计算 (Acc/F1/AUC/等)
  - 混淆矩阵
  - ROC/PR 曲线
  - 分类报告

可视化分析:
  - 注意力图可视化
  - 特征图可视化
  - 激活图可视化
  - 错误样本分析
  - 模型解释 (Grad-CAM)

性能分析:
  - 推理速度测试
  - 内存占用分析
  - FLOPs 计算
  - 模型复杂度分析
```

#### 6. 模型优化模块

```yaml
模型压缩:
  - 量化 (INT8/FP16)
  - 剪枝 (结构化/非结构化)
  - 知识蒸馏
  - 压缩效果对比

超参数优化:
  - 网格搜索
  - 随机搜索
  - 贝叶斯优化
  - 自动化调优

模型融合:
  - 模型集成
  - 权重平均
  - 投票机制
```

#### 7. 模型导出模块

```yaml
导出格式:
  - PyTorch (.pth)
  - ONNX (.onnx)
  - TorchScript (.pt)
  - TensorRT (优化)

导出配置:
  - 输入形状
  - 动态轴
  - 优化级别
  - 目标平台

验证:
  - 导出模型验证
  - 精度对比
  - 速度对比
```

#### 8. 部署推理模块

```yaml
推理服务:
  - 本地推理
  - 批量推理
  - 实时推理 API
  - 推理性能监控

部署选项:
  - Docker 容器
  - REST API
  - gRPC 服务
  - 边缘设备部署

推理优化:
  - 批处理
  - 模型缓存
  - 预处理优化
  - 后处理优化
```

#### 9. 实验管理模块

```yaml
实验追踪:
  - 自动记录所有实验
  - 实验参数记录
  - 实验结果记录
  - 实验标签和备注

实验对比:
  - 多实验对比
  - 参数差异分析
  - 指标对比图表
  - 最佳实验推荐

实验复现:
  - 一键复现实验
  - 配置导出/导入
  - 环境记录
  - 随机种子管理

报告生成:
  - 自动生成实验报告
  - Markdown/PDF 导出
  - 图表嵌入
  - 分享链接
```

---

## 🐳 Docker 部署方案

### Docker 架构

```yaml
服务组成:
  - frontend: React 前端 (Nginx)
  - backend: FastAPI 后端
  - redis: Redis 缓存和消息队列
  - postgres: PostgreSQL 数据库 (可选)
  - celery-worker: Celery 工作进程
  - celery-beat: Celery 定时任务 (可选)

网络:
  - medfusion-network: 内部网络

卷:
  - data: 数据集存储
  - models: 模型存储
  - logs: 日志存储
  - postgres-data: 数据库数据 (可选)
```

### docker-compose.yml

```yaml
version: '3.8'

services:
  # 前端服务
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - medfusion-network
    restart: unless-stopped

  # 后端服务
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./medfusion.db
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/1
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./logs:/app/logs
    depends_on:
      - redis
    networks:
      - medfusion-network
    restart: unless-stopped
    # GPU 支持
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  # Redis 服务
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    networks:
      - medfusion-network
    restart: unless-stopped

  # Celery Worker
  celery-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: celery -A app.celery_app worker --loglevel=info
    environment:
      - DATABASE_URL=sqlite:///./medfusion.db
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/1
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./logs:/app/logs
    depends_on:
      - redis
      - backend
    networks:
      - medfusion-network
    restart: unless-stopped
    # GPU 支持
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

networks:
  medfusion-network:
    driver: bridge

volumes:
  data:
  models:
  logs:
```

### 快速启动

```bash
# 1. 克隆项目
git clone https://github.com/your-org/medfusion-web.git
cd medfusion-web

# 2. 启动所有服务
docker-compose up -d

# 3. 查看日志
docker-compose logs -f

# 4. 访问 Web UI
# 浏览器打开: http://localhost:3000

# 5. 停止服务
docker-compose down

# 6. 重启服务
docker-compose restart

# 7. 更新服务
docker-compose pull
docker-compose up -d --build
```

---

## 🔌 扩展性设计

### 插件系统

```python
# 插件接口
class NodePlugin:
    """节点插件基类"""
    
    @property
    def name(self) -> str:
        """插件名称"""
        pass
    
    @property
    def category(self) -> str:
        """节点类别"""
        pass
    
    @property
    def inputs(self) -> list[str]:
        """输入端口"""
        pass
    
    @property
    def outputs(self) -> list[str]:
        """输出端口"""
        pass
    
    def execute(self, inputs: dict) -> dict:
        """执行节点逻辑"""
        pass
    
    def validate(self, config: dict) -> bool:
        """验证配置"""
        pass

# 注册插件
from app.core.node_registry import register_node

@register_node
class CustomDataLoader(NodePlugin):
    name = "Custom Data Loader"
    category = "data"
    inputs = ["path"]
    outputs = ["dataset"]
    
    def execute(self, inputs):
        # 自定义逻辑
        return {"dataset": ...}
```

### API 扩展

```python
# 自定义 API 端点
from fastapi import APIRouter

router = APIRouter(prefix="/api/custom", tags=["custom"])

@router.get("/my-endpoint")
async def my_custom_endpoint():
    return {"message": "Custom endpoint"}

# 注册到主应用
from app.main import app
app.include_router(router)
```

### 前端扩展

```typescript
// 自定义节点组件
import { NodePlugin } from '@/types/node';

export const CustomNode: NodePlugin = {
  type: 'custom-node',
  category: 'custom',
  component: CustomNodeComponent,
  config: {
    // 节点配置
  },
};

// 注册节点
import { registerNode } from '@/core/nodeRegistry';
registerNode(CustomNode);
```

---

## 📊 性能优化策略

### 前端性能优化

```yaml
渲染优化:
  - React.memo (避免不必要的重渲染)
  - useMemo/useCallback (缓存计算结果)
  - 虚拟滚动 (大量节点)
  - Canvas 渲染 (节点连接线)
  - 懒加载组件

数据优化:
  - 数据采样 (图表数据过多时)
  - 增量更新 (只更新变化的数据)
  - 防抖/节流 (频繁更新)
  - IndexedDB 缓存 (本地数据)

网络优化:
  - WebSocket 消息压缩
  - 批量请求
  - 请求缓存
  - CDN 加速

代码优化:
  - 代码分割 (按路由)
  - Tree Shaking
  - 压缩混淆
  - Gzip 压缩
```

### 后端性能优化

```yaml
异步处理:
  - 异步 I/O (asyncio)
  - 异步数据库查询
  - 异步文件操作
  - 并发请求处理

缓存策略:
  - Redis 缓存热数据
  - 响应缓存
  - 查询结果缓存
  - 静态资源缓存

数据库优化:
  - 连接池
  - 索引优化
  - 查询优化
  - 批量操作

任务队列:
  - 优先级队列
  - 任务分片
  - 结果缓存
  - 失败重试
```

### 通信优化

```yaml
WebSocket:
  - 消息压缩 (zlib)
  - 消息批处理
  - 心跳检测
  - 自动重连

数据传输:
  - 增量更新
  - 二进制传输 (大数据)
  - 流式传输
  - 断点续传
```

---

## 🎯 开发优先级

### Phase 1: 核心框架 (1 周)

```yaml
后端:
  - [x] FastAPI 项目搭建
  - [x] 数据库模型设计
  - [x] 基础 API 端点
  - [x] WebSocket 通信
  - [x] Celery 任务队列

前端:
  - [x] React 项目搭建
  - [x] 主题系统
  - [x] 基础布局
  - [x] 路由配置
  - [x] API 客户端

集成:
  - [x] 前后端联调
  - [x] Docker 配置
  - [x] 开发环境搭建
```

### Phase 2: 工作流系统 (1 周)

```yaml
节点系统:
  - [x] 节点注册表
  - [x] 节点执行引擎
  - [x] 10+ 核心节点

工作流编辑器:
  - [x] React Flow 集成
  - [x] 节点库面板
  - [x] 属性配置面板
  - [x] 工作流保存/加载

执行引擎:
  - [x] 工作流验证
  - [x] 工作流执行
  - [x] 错误处理
```

### Phase 3: 训练系统 (1 周)

```yaml
训练功能:
  - [x] 训练任务管理
  - [x] 实时监控
  - [x] 训练控制 (暂停/恢复/停止)

监控面板:
  - [x] 训练曲线
  - [x] 系统资源监控
  - [x] 日志输出

数据管理:
  - [x] 数据集上传
  - [x] 数据集预览
  - [x] 数据预处理
```

### Phase 4: 完善功能 (1-2 周)

```yaml
模型管理:
  - [x] 模型库
  - [x] 模型导出
  - [x] 模型部署

实验管理:
  - [x] 实验追踪
  - [x] 实验对比
  - [x] 报告生成

优化:
  - [x] 性能优化
  - [x] UI/UX 优化
  - [x] 文档完善
```

---

## 📝 总结

### 技术亮点

1. **高性能**: 异步后端 + 优化前端 + 缓存策略
2. **高度可定制**: 主题切换 + 设计风格切换 + 布局自定义
3. **一站式**: 完整的 ML 工作流，从数据到部署
4. **易部署**: Docker 一键启动，本地运行友好
5. **可扩展**: 插件系统 + 开放 API + 自定义节点

### 性能指标目标

```yaml
前端:
  - 首屏加载: < 2s
  - 节点渲染: 1000+ 节点流畅
  - 图表更新: 60 FPS
  - 内存占用: < 500MB

后端:
  - API 响应: < 100ms (P95)
  - WebSocket 延迟: < 50ms
  - 并发请求: 1000+ QPS
  - 内存占用: < 2GB

训练:
  - 监控延迟: < 1s
  - 日志延迟: < 500ms
  - 资源监控: 实时更新
```

---

**下一步**: 开始搭建项目骨架？

我可以立即开始创建项目结构和核心代码。你觉得如何？
